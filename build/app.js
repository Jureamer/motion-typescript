"use strict";
{
    class BlockMakerImpl {
        constructor() {
            this.block = [];
        }
        addVideo(video) {
            document.querySelector;
            this.block.push(video);
        }
        addPicture(picture) {
            this.block.push(picture);
        }
        addNote(note) {
            this.block.push(note);
        }
        addTodo(todo) {
            this.block.push(todo);
        }
        deleteOne() {
        }
    }
    const blockMaker = new BlockMakerImpl();
}
